Giải thích sự khác biệt cơ bản về vị trí lưu trữ trong JVM (Heap vs Metaspace) giữa Instance members và Static members.

JVM nạp Class và khởi tạo vùng nhớ cho các biến static vào thời điểm nào trong vòng đời của ứng dụng?

Tại sao sử dụng static final cho các dữ liệu cấu hình dùng chung lại giúp tiết kiệm bộ nhớ đáng kể trong ứng dụng Java quy mô lớn?

Tại sao việc lạm dụng từ khóa static cho các biến lưu trữ trạng thái đối tượng lại dễ dẫn đến lỗi 'nhất quán dữ liệu' (State Inconsistency)?

Làm sao để xác định một thuộc tính nên là static hay instance dựa trên việc thuộc tính đó có thay đổi theo từng thực thể đối tượng hay không?
