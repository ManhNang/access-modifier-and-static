Giải thích sự khác biệt về vùng nhớ (Metaspace so với Heap) khi lưu trữ Static members và Instance members trong JVM

JVM nạp Class và khởi tạo vùng nhớ cho các biến static vào thời điểm nào trong vòng đời ứng dụng?

Khi nào cần sử dụng khối khởi tạo tĩnh static và lợi ích của nó trong việc thiết lập cấu hình dùng chung?

Tại sao lạm dụng từ khóa static cho các hàm xử lý logic lại dễ gây ra lỗi rò rỉ dữ liệu chéo giữa các thực thể?

Làm sao để xác định một phương thức có nên để là static hay không dựa trên việc nó có truy cập vào trạng thái (instance fields) hay không?
